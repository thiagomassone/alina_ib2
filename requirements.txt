# ─── Instalar en este orden para evitar conflictos ───────────────────────────
# IMPORTANTE: correr "pip install -r requirements.txt" desde la raíz del repo

# ─── Backend (API + DB) ───
fastapi==0.115.0
uvicorn[standard]==0.32.0
sqlalchemy==2.0.35
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.3.0
typing-extensions>=4.8.0

# ─── Autenticación (bcrypt ANTES de passlib) ───
bcrypt==4.0.1
passlib==1.7.4
python-jose[cryptography]==3.3.0
python-multipart==0.0.12

# ─── Frontend (GUI multiplataforma) ───
flet==0.24.1
httpx==0.27.2

# ─── WebSocket (comunicación con ESP32) ───
websocket-client==1.8.0

# ─── Dev / testing (opcional) ───
pytest==8.3.3